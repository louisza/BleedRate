# Python dependencies
# NOTE: starlette 1.0.0 broke TemplateResponse API (dict context → new signature)
# Pin until we migrate all call sites to new Starlette 1.x API
fastapi>=0.100.0,<0.120.0
starlette>=0.27.0,<1.0.0
uvicorn[standard]
jinja2>=3.1.0,<4.0.0
pydantic>=2
pyyaml
sqlmodel
python-dotenv
itsdangerous
python-multipart
httpx
supabase

# Testing dependencies
pytest
pytest-cov
pytest-xdist  # Parallel testing
pytest-asyncio  # Async test support
pytest-watch  # Watch mode for tests

# Code quality
ruff
black
pre-commit  # Git hooks for automation
bandit  # Security testing
